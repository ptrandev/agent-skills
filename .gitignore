# macOS Finder metadata — never source
.DS_Store

# Real credentials — this repo is PUBLIC. The committed *.example.md files are the
# templates; the filled-in copies never leave the machine.
skills/full-send/dev-credentials.md
skills/ui-walkthrough/dev-credentials.md

# phillip-sync writes its per-repo cooldown cursor here at runtime (via the symlink) — local state, not source
skills/phillip/.sync-state.json

# Private review rows: repo-specific rules mined from private PRs. The shareable core of the
# rubric lives in skills/phillip/RUBRIC.md, which IS tracked.
skills/phillip/RUBRIC.private.md

# Generated repository-local host adapters
.agents/skills/
.claude/skills/
.gstack/
